#!/usr/bin/python3

"tuple with length of dtring and first character"

def multiple_returns(sentence):
    if not sentence:
        return none
    else:
        length = len(sentence)
        first = sentence[0]
        tuple_1 = length, first
        return tuple_1
